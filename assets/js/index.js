const electron = require('electron')

document.body.addEventListener('click', (event) => {
   /* var el = event.target;
    if (el.id == "okBtn"){
        var te = document.getElementById('path').value;
        console.log(te);
    }*/
});