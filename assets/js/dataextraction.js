window.Rigsarkiv = window.Rigsarkiv || {},
function (n) {
    const {ipcRenderer} = require('electron')

    Rigsarkiv.DataExtraction = {        
        initialize: function () {
        }
    };
}(jQuery);